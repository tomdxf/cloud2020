server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 120.26.86.157:1111  # nacos作为服务注册中心
      config:
        server-addr: 120.26.86.157:1111 # nacos作为服务配置中心
        file-extension: yaml # 指定yaml 格式的配置
        #group: DEV_GROUP #默认default_group 可根据需求调整
        #namespace: